const btn=document.getElementById('generate');const prompt=document.getElementById('prompt');const style=document.getElementById('style');const result=document.getElementById('result');
btn.addEventListener('click',()=>{const text=prompt.value.trim();if(!text){result.classList.remove('hidden');result.textContent='Please describe the video you want to create.';return;}result.classList.remove('hidden');result.innerHTML='<strong>Video concept ready 🎬</strong><br><br>Style: '+style.value+'<br>Prompt: '+text+'<br><br>Your AI video generation backend can be connected to the secure API endpoint later.';});
